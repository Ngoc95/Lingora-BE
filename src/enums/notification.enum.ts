export enum NotificationType {
  CHANGE_PASSWORD = 'Change password',
  LIKE = 'Like',
  COMMENT = 'Comment',
  ORDER = 'Order'
}

export enum NotificationTarget {
  ALL = 'All',
  ONLY_USER = 'Only user',
  SEGMENT = 'Segment'
}
