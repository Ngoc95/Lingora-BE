export enum ClassroomLessonType {
    VIDEO = 'VIDEO',         // Bài học video/podcast
    STUDYSET = 'STUDYSET',   // Bộ flashcard/quiz
    TEXT = 'TEXT',           // Nội dung văn bản
    MIXED = 'MIXED',         // Kết hợp nhiều loại
}
