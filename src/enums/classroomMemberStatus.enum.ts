export enum ClassroomMemberStatus {
    PENDING = 'PENDING',   // Chờ giáo viên duyệt
    ACTIVE = 'ACTIVE',     // Đang học
    REMOVED = 'REMOVED',   // Bị giảng viên xóa khỏi lớp
    LEFT = 'LEFT',         // Tự rời khỏi lớp
}
