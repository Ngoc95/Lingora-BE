export enum ClassroomChatMessageType {
    TEXT = 'TEXT',       // Tin nhắn văn bản
    IMAGE = 'IMAGE',     // Hình ảnh
    FILE = 'FILE',       // Tệp đính kèm
    SYSTEM = 'SYSTEM',   // Thông báo hệ thống (vào/ra lớp, v.v.)
}
